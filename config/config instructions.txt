Criar o arquivo parameters.js que deve expor a string que usada para assinar o token, a porta do servidor e a url do banco.
Algo como:
module.exports = {
    secret: 'yourKey',
    port: process.env.PORT || '8080',
    urlDb: process.env.MONGOLAB_URI || 'mongodb://localhost:27017/your_collection'
}